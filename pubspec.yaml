name: twizzy_admin
description: "Twizzy Admin Dashboard - Web Application"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # State Management
  provider: ^6.1.1
  
  # HTTP Client
  http: ^1.2.0
  
  # Charts
  fl_chart: ^0.69.0
  
  # Secure Storage (for web, uses localStorage)
  shared_preferences: ^2.2.2
  
  # Intl for date formatting
  intl: ^0.19.0

  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
